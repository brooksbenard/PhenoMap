# Resolve group labels to a vector aligned with cell_ids

Resolve group labels to a vector aligned with cell_ids

## Usage

``` r
resolve_group_labels(
  group_labels,
  cell_ids,
  group_column = NULL,
  cell_id_column = "cell_id"
)
```
