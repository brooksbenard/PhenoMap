# Get Cell Type Color Palette

Returns a named vector of colors for the given cell types. Uses
`cell_type_colors` for known types; assigns fallback colors for others.

## Usage

``` r
get_celltype_palette(types)
```

## Arguments

- types:

  Character vector of cell type names.

## Value

Named character vector of hex colors.
